// TODO: 重写测试适配新 ActivityEvent 类型（raspi-eye-events 表结构）
// 旧测试引用 sessionId, detectedClass, kvsStartTimestamp 等已移除字段
import { describe, it, expect } from 'vitest';

describe('EventsPanel', () => {
  it('placeholder — tests pending rewrite', () => {
    expect(true).toBe(true);
  });
});
